# Variables de entorno para el proyecto.
# Copiá este archivo como .env y completá los valores.
# Las variables con prefijo VITE_ son accesibles en el cliente (Firebase).
# ANTHROPIC_API_KEY NO tiene prefijo VITE_ — solo existe en el servidor (/api/claude.js).

VITE_FIREBASE_API_KEY=tu-api-key-de-firebase
VITE_FIREBASE_AUTH_DOMAIN=tu-proyecto.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=tu-proyecto-id

# Solo en Vercel (panel de env vars), nunca en .env:
# ANTHROPIC_API_KEY=sk-ant-...
